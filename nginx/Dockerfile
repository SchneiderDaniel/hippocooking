# Use the Nginx image
# Use the official Nginx image from the Docker Hub
FROM nginx:stable

# Copy your custom Nginx configuration file (nginx.conf) into the container
# You can customize this file according to your needs
COPY nginx.conf /etc/nginx

